# Technical Context

## Technologies Used
- **Python**: Inferred core language (presence of `.venv`, `requirements.txt`).
- **Data Formats**: 
    - JSON (`grounded_reports_20240819.json`)
    - CSV (`master_table.csv`)
    - ZIP (`master_table.csv.zip`)
- **Git**: Version control (initialized but empty).

## Development Setup
- Virtual Environment: `.venv` exists.
- Dependencies: `requirements.txt` (currently empty, needs population).
- Dataset location: `dataset/` directory.

## Technical Constraints
- Large dataset handling (PadChest is large).
- Image processing requirements (likely need libraries like `pillow`, `opencv`, `numpy`, `pandas`).
